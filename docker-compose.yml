version: '3.8'

services:
    frontend-committee:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3110:3000'
        environment:
            NODE_ENV: production # Set your desired Node environment

